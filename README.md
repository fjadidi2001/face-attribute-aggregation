# face-attribute-aggregation
A microservices-based Face Attribute Aggregation System using gRPC, which detects faces, extracts landmarks, estimates age and gender, and stores attributes efficiently using Redis and JSON.
